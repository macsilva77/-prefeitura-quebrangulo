# Sistema de Controle de Gastos - Variáveis de Ambiente
# Copie este arquivo para .env e configure conforme necessário

# ========== AMBIENTE ==========
NODE_ENV=development
# Valores: development, staging, production

# ========== SEGURANÇA ==========
# Em produção, use HTTPS obrigatoriamente
USE_HTTPS=false
# URL segura para API
API_BASE_URL=http://localhost:8000

# ========== AUTENTICAÇÃO ==========
# Tempo de sessão em minutos
SESSION_TIMEOUT=30
# Máximo de tentativas de login falhadas
MAX_LOGIN_ATTEMPTS=5
# Bloqueio de conta em minutos após tentativas excedidas
LOCKOUT_DURATION=15

# ========== LOGS ==========
# Level: debug, info, warn, error
LOG_LEVEL=info
# Manter apenas últimos N logs
MAX_AUDIT_LOGS=1000

# ========== FILAS & RATE LIMITING ==========
# Máximo de requisições por minuto (quando houver API)
RATE_LIMIT_PER_MINUTE=60
# Tamanho máximo de arquivo upload em MB
MAX_FILE_SIZE=10

# ========== EMAIL (Futuro) ==========
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=seu@email.com
# SMTP_PASS=sua-senha
# EMAIL_FROM=noreply@prefeitura.gov.br

# ========== BANCO DE DADOS (Futuro) ==========
# DATABASE_URL=postgresql://user:password@localhost:5432/prefeitura
# DB_POOL_SIZE=20

# ========== 2FA (Futuro) ==========
# ENABLE_2FA=false
# 2FA_ISSUER=Prefeitura de Quebrangulo

# ========== BACKUP (Futuro) ==========
# BACKUP_ENABLED=true
# BACKUP_FREQUENCY=daily
# BACKUP_STORAGE=s3
